/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package mockinator.app;

import mockinator.list.LinkedList;

import static mockinator.utilities.StringUtils.join;
import static mockinator.utilities.StringUtils.split;
import static mockinator.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
